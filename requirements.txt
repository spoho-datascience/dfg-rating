# dfg-rating runtime requirements (curated top-level intent).
#
# Installs cleanly on Python 3.12. The previous frozen 174-line lock pinned
# C-extension packages (numpy 1.21 / pandas 1.3 / scipy 1.7 ...) that have no
# 3.12 wheels and fail to build; this file modernizes that stack and pins only
# the version-sensitive Dash 1.x UI to keep its split-package imports working.
#
# For an exact reproducible install use requirements.lock instead.
# Optional subsystems are also declared as setup.py extras:
#   pip install .              # core only
#   pip install .[viz,db,dev]  # everything below
#
# Note: configuration_model_mcmc (used only by dfg_rating/tests/random_network.py)
# is intentionally omitted -- its sdist pins an ancient numpy that cannot build on
# Python 3.12. Install it manually if you need that test helper.

# --- core / model (also covers experiments/player_elo_export notebook) ---
numpy>=2.0
pandas>=2.2
scipy>=1.11
scikit-learn>=1.4
networkx>=3.2
tqdm>=4.60
click>=7.1,<8            # held <8: Dash 1.x / Flask 1.x stack below needs click 7

# --- I/O for the .xlsx match data ---
openpyxl>=3.1
xlrd>=2.0

# --- statistics ---
statsmodels>=0.14

# --- plotting / notebook ---
matplotlib>=3.8
plotly>=5.0
notebook>=6.4,<7         # 6.x keeps Jinja2 2.x compatibility with Dash 1.x
nbconvert>=6.0,<6.5      # 6.5+ requires Jinja2>=3, which Flask 1.x rejects
ipykernel>=6.0
ipywidgets>=7,<8

# --- viz / UI (Dash 1.x: pure-python, pinned to preserve split-package imports) ---
dash==1.20.0
dash-bootstrap-components==0.10.7
dash-core-components==1.16.0
dash-html-components==1.1.3
dash-table==4.11.3
dash-renderer==1.9.1
dash-cytoscape==0.2.0
dash-daq==0.5.0
jupyter-dash==0.4.0
halo>=0.0.31
# Dash 1.x requires the Flask 1.x world; these pins keep that stack consistent.
Flask>=1.1,<2
Werkzeug>=1.0,<2
Jinja2>=2.11,<3
MarkupSafe>=2.0,<2.1
itsdangerous>=1.1,<2
# Dash 1.20 imports pkg_resources, removed in setuptools 81.
setuptools<81

# --- database ---
psycopg2-binary>=2.9

# --- dev / test ---
pytest>=7.0
